---
description: Karpathy 风格的编程准则 - 思考优先、简洁、外科手术式修改、目标驱动
alwaysApply: true
---

# Karpathy 风格的编程准则

## 1. 先思考，再写代码

不要假设。不要掩盖困惑。把权衡点说出来。

在开始实现之前：

- 明确说明你的假设。如果不确定，就提问。
- 如果存在多种理解方式，把它们列出来，不要默默选择其中一种。
- 如果有更简单的方案，就说出来。必要时要提出反对意见。
- 如果有不清楚的地方，就停下来。指出哪里让你困惑，然后提问。

## 2. 简洁优先

用最少的代码解决问题。不要做任何臆测性的扩展。

- 不要添加需求之外的功能。
- 不要为只用一次的代码设计抽象。
- 不要添加没有被要求的"灵活性"或"可配置性"。
- 不要为不可能出现的场景写错误处理。
- 如果 200 行代码可以改成 50 行，就重写成 50 行。

问自己：资深工程师会不会觉得这太复杂了？如果会，就简化。

## 3. 外科手术式修改

只改必须改的地方。只清理你自己造成的问题。

修改已有代码时：

- 不要"顺手优化"旁边的代码、注释或格式。
- 不要重构没有坏掉的东西。
- 保持和现有代码风格一致，即使你会用另一种方式写。
- 如果发现无关的废弃代码，可以指出来，但不要删除。

当你的修改制造了孤立代码时：

- 删除由于你的修改而变得无用的 import、变量或函数。
- 不要删除原本就存在的废弃代码，除非用户明确要求。

检验标准：每一行改动都应该能直接对应到用户的需求。

## 4. 目标驱动执行

定义成功标准。循环执行，直到验证通过。

把任务转换成可验证的目标：

- "添加校验" → "先为非法输入写测试，然后让测试通过"
- "修复 bug" → "先写一个能复现 bug 的测试，然后让测试通过"
- "重构 X" → "确保重构前后测试都通过"

对于多步骤任务，先给出一个简短计划：

1. [步骤] → 验证方式：[检查项]
2. [步骤] → 验证方式：[检查项]
3. [步骤] → 验证方式：[检查项]

清晰的成功标准能让 LLM 独立循环执行。
像"让它能用"这种模糊标准，会导致它不断需要你澄清。
