## Summary

Implemented automatically by `github-issue-worker` for issue #{number}.

> {title}

## What changed

{diffstat}

## Verification

Worker ran the following project checks:

{checks}

> All checks above were attempted by the worker. They are NOT a substitute for human review.

## Agent

- Backend: `{agent}`
- Branch: `{branch}` (based on `{base_branch}`)
- Source issue: {issue_url}

## Reviewer notes

- This PR was opened by an autonomous worker. Treat the diff with appropriate skepticism.
- If anything looks wrong, leave a review comment and re-label the source issue with `agent-todo` to send the worker back at it.

Closes #{number}
